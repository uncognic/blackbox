.asm
.define $VAL1 10
.define $VAL2 11

.main

MOV r0, $VAL1 + $VAL2
PRINTREG r0        ; expect 21
NEWLINE

MOV r0, $VAL1 * $VAL2
PRINTREG r0        ; expect 110
NEWLINE

MOV r0, ($VAL1 + 5) * 2
PRINTREG r0        ; expect 30
NEWLINE

HLT OK