%asm

%macro loop_n cnt
    MOVI R00, $cnt
.@@loop:
    PRINT '.'
    DEC R00
    MOVI R01, 0
    CMP R00, R01
    JNE @@loop
%endmacro

%macro hello
    write stdout "Hello from macro!"
    NEWLINE
%endmacro

%main
    %loop_n 3
    %hello

    HALT OK