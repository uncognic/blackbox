# Blackbox
Blackbox is an assembly-like language with an assembler and a bytecode interpreter. It uses Intel assembly syntax. (register order is dst, src)
### Examples
See [examples.md](docs/examples.md)
### Building
Run `make` in the directory of either the interpreter or the assembler. `make clean` to delete any residual files from `make`

